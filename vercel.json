{
    "version": 2,
    "builds": [
        {
            "src": "api/**/*.ts",
            "use": "@vercel/node"
        },
        {
            "src": "package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "dist/public",
                "buildCommand": "npm run build:client"
            }
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api/[[...route]].ts"
        },
        {
            "src": "/(.*)",
            "dest": "dist/public/index.html"
        }
    ],
    "installCommand": "apt-get update && apt-get install -y cmake && npm install"
}